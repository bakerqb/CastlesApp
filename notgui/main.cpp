#include <iostream> 
#include <iomanip>
#include <vector>
#include <deque> 
#include <stack> 
#include <algorithm> 
#include <ofstream> 
#include <ostream> 
#include <fstream> 
#include "castles.cpp"
int main(int argc, char** argv){
    ios::sync_with_stdio(false);
    g = game()
    p100 = g.readIn(100); 
    p150 = g.readIn(150); 
    p200 = g.readIn(250); 
    p300 = g.readIn(300); 
    p350 = g.readIn(350); 
    p400 = g.readIn(400); 
    p500 = g.readIn(500); 
    p600 = g.readIn(600); 
}