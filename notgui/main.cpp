#include <iostream> 
#include <iomanip>
#include <vector>
#include <deque> 
#include <stack> 
#include <algorithm> 
#include <ofstream> 
#include <ostream> 
#include <fstream> 
#include "castles.cpp"
int main(int argc, char** argv){
    ios::sync_with_stdio(false);
    p100 = readIn(100); 
    p150 = readIn(150); 
    p200 = readIn(250); 
    p300 = readIn(300); 
    p350 = readIn(350); 
    p400 = readIn(400); 
    p500 = readIn(500); 
    p600 = readIn(600); 
}